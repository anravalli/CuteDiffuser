# CuteDiffuser
A simple Python graphical application to help people play around with pre-trained models available on Huggingface, through their __diffusers__ library.

# Install
Support Python 3.11: using Python 3.13, the __tokenizers_ package fail to install through _pip_ due to a build error
